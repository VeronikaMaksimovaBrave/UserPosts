import { makeStyles } from '@material-ui/core/styles'

const useStyles = makeStyles({
    root: {
        width: '100%',
        maxWidth: '1024px',
        margin: 'auto',
        marginTop: '20px'
        
    },
    container: {
        maxHeight: 590,
    },
})

export default useStyles