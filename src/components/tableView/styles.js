import { makeStyles } from '@material-ui/core/styles'

const useStyles = makeStyles({
    root: {
        width: '70%',
        
    },
    container: {
        maxHeight: 590,
    },
})

export default useStyles